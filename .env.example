# Stress test API keys (benchmark/stress_run.py).
# Copy this file to `.env` in the repo root and fill in values:
#   cp .env.example .env
# `.env` is gitignored. Alternatively export these in your shell.

ANTHROPIC_API_KEY=
OPENAI_API_KEY=
GOOGLE_API_KEY=
